#!/usr/bin/python3

#Probes the connectivity to given address-port-tuple via connection type protocol and returns elapsed time. If a protocol other than https or tls is given, function probes a simple tcp socket connection.
def Actions():
	print("Hello world!")

if __name__ == '__main__':
	Actions()
